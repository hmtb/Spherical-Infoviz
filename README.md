# Setting Up

Prepare:
- Works on Windows 10 / Mac OS X (Vive mode only on Mac)
- Install nodejs 6.x LTS

Install dependencies:

    npm install

Build:

    npm run build

Run:

    npm start